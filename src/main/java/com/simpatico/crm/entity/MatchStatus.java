package com.simpatico.crm.entity;

/**
 * Enum representing the status of a potential lead-to-inventory match.
 */
public enum MatchStatus {
    INITIAL,
    REVIEWED,
    PRESENTED,
    ACCEPTED,
    REJECTED,
    EXPIRED
}
